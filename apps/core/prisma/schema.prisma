generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

/// Trivial model proving Prisma owns migrations; real L1 models come later.
model Setting {
  id    String @id @default(uuid())
  key   String @unique
  value String
}
